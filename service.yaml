apiVersion: v1
kind: Service
metadata:
  name:  webapp-service
  namespace: webapp
spec:
  type: NodePort
  selector:
    app: webapp
  ports:
  - nodePort: 30082
    port: 8000
    targetPort: 8080